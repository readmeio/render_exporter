{
  "name": "render_exporter",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "watch": "nodemon --watch './**/*.ts' --ignore 'dist/**' --ext 'ts' --exec 'npm run build && node dist/index.js || echo \"TS compilation error, waiting for changes...\"'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@llimllib/renderapi": "^1.0.0",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "pino-pretty": "^13.0.0",
    "typescript": "^5.7.3"
  }
}
